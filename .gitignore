__pycache__/
*.pyc
.agentctl/
*.duckdb
*.duckdb.wal
.venv/
*.egg-info/
build/
dist/
# generated gRPC stubs are VENDORED (committed) so a fresh clone / Docker build needs no protoc.
.pytest_cache/
# local-only project explainer (not part of the published repo)
OVERVIEW.md
