# dummy_vision_agent — example agent for `agentctl push`
name: dummy-vision-agent
model: claude-opus-4-8
task: bottle-detection
entrypoint: agent.py
eval:
  suite: vision-accuracy
  samples: 300
  win_rate: 0.62      # simulated quality vs the live baseline (>0.50 = improvement -> MERGE)
  tie_rate: 0.08
