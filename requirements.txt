# agentctl — locked Python dependencies (control plane + CLI).
# Pinned to a known-good set; regenerate with `pip install -e . && pip freeze`.
grpcio==1.81.1
grpcio-tools==1.81.1
duckdb==1.5.3
psycopg[binary]==3.3.4
websockets==16.0
uvicorn==0.49.0
opentelemetry-sdk==1.42.1
scipy==1.17.1
numpy==2.4.6
fastapi==0.136.3
typer==0.20.0
rich==14.2.0
