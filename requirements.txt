# agentctl prototype dependencies (Python-first).
# scipy / numpy / fastapi are already present in the base env; pinned here for clarity.
grpcio>=1.60
grpcio-tools>=1.60
duckdb>=0.10
psycopg[binary]>=3.1
websockets>=12.0
uvicorn>=0.27
opentelemetry-sdk>=1.24
# already-present, listed for reproducibility:
scipy>=1.11
numpy>=1.26
fastapi>=0.110
